Change vector syntax from %(a b c) to [a b c].
Change file ext.
Change (load "name.ext") to (import name).
Implement unquote-splicing.
Implement chars.

Done:
Change vector syntax from #(...) to %(...).
Change comment syntax from ; to #.
