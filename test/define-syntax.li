(let ()
  (import match)
  (define-macro
    define-syntax
    (lambda (expr _)
      (let ((name (cadr expr))
            (rules (caddr expr)))
        `(define-macro
           ,name
           (lambda (expr _)
             (*match* (cons ',name (cdr expr)) ',(cddr rules)))))))
  (export define-syntax))
